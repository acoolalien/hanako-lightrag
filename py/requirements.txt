# hanako-lightrag Python dependencies
# Install: pip install -r requirements.txt

lightrag-hku>=1.0.0
openai>=1.0.0
numpy>=1.24.0
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# PostgreSQL backend (optional, lightrag-hku auto-installs if needed)
asyncpg>=0.29.0
pgvector>=0.3.0
